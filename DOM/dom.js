document.getElementById("title");
element.style.color = "green";

element.innerHTML; // give the full text with hmtl tags also
element.textContent; //give the content which is visible
element.innerText; // give the full content which is not visible
element.querySelector("h2"); //only gives the first h2 it will get
element.querySelectorAll("h2"); //only all the h2 int the HTMl page


